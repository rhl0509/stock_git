# 네이티브(Capacitor) 빌드 전용 — 앱이 호출할 배포 서버 절대 URL.
# 웹 빌드는 같은 오리진에서 서빙되므로 불필요(상대경로 '/').
# 네이티브 앱은 capacitor://localhost 에서 돌아 API 서버가 없으므로 반드시 설정해야 한다.
# 예) VITE_API_BASE=https://api.example.com
VITE_API_BASE=
