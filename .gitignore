# ── 비밀정보 ──
.env
*.env
!.env.example

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/

# ── Node ──
node_modules/

# ── 데이터/모델/캐시 (대용량·재생성 가능) ──
cache/
data/
logs/
backup/
# DB 덤프(회원 데이터 포함) — 절대 커밋 금지
_db_backup/
# CatBoost 학습 로그
catboost_info/
XGBoost_v2/data/
# 아래 *.cbm 등은 model/ 직속만 매칭하고 하위 폴더는 안 걸린다. 백업/절제 폴더를
# 명시적으로 제외한다(재학습 산출물이라 재생성 가능, 커밋 대상 아님).
XGBoost_v2/model/_backup_*/
XGBoost_v2/model/_ablation_*/
XGBoost_v2/model/_pit_result_*/
XGBoost_v2/model/*.cbm
XGBoost_v2/model/*.pkl
XGBoost_v2/model/*.txt
XGBoost_v2/model/xgb_*.json
XGBoost_v2/model/lgb_*.txt
XGBoost_v2/model/recommend_history/
XGBoost_v2/model/recommend_history_abs/
XGBoost_v2/model/daily_recommend*.json
XGBoost_v2/model/ml_lr_cache.json
trading/model/

# ── 빌드 산출물 ──
static/spa/

# 시크릿 파일의 사본이 추적되지 않도록 (.env 는 막히지만 .env.bak_20260717 은 안 막힌다)
*.bak
*.bak_*
