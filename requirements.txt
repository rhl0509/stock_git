# 실행 환경(d:\expense_tracker\.venv64) 기준으로 핀 고정 — 2026-06-10
# 업그레이드 시: 버전 변경 → 모델 파일(pkl/cbm) 재생성 여부 확인 필수

# ── 웹 프레임워크 ──
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
python-dotenv==1.2.2
itsdangerous==2.2.0
werkzeug==3.1.8         # password hashing (generate_password_hash / check_password_hash)
bcrypt==5.0.0           # 레거시 $2b$ 해시 검증
jinja2==3.1.6

# ── 데이터베이스 ──
pymysql==1.1.2
cryptography==47.0.0
dbutils==3.1.2

# ── AI (Claude) ──
anthropic==0.97.0

# ── 주식 데이터 ──
yfinance==1.3.0
pykrx==1.2.7
finance-datareader==0.9.110
fredapi==0.5.2

# ── 데이터 분석 ──
pandas==2.3.3
numpy==2.4.4
pyarrow==24.0.0
xgboost==3.2.0
lightgbm==4.6.0
catboost==1.2.10
shap==0.51.0
optuna==4.8.0
scikit-learn==1.8.0
scipy==1.17.1
matplotlib==3.10.9

# ── 크롤링 ──
requests==2.33.1
beautifulsoup4==4.14.3
lxml==6.1.0

# ── 기타 ──
pytz==2026.1.post1
apscheduler==3.11.2

# ── 테스트 ──
pytest==9.0.3
