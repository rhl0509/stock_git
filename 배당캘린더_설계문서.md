# 배당·현금흐름 캘린더 — 기능 설계 문서

> 기존 **가계부 프로그램** · **주식 프로그램** 과 연동되는 세 번째 프로그램.
> 보유 주식의 배당 지급일과 예상 배당금을 캘린더로 보여주고,
> 배당금을 가계부의 "수입"으로 자동 반영한다.

---

## 1. 개요

가계부(현금흐름)와 주식(투자)을 잇는 다리 역할의 프로그램이다.
주식 → 가계부 방향으로 데이터가 흐르는 구조라, 기존 두 프로그램과 함께
전체 시스템이 양방향으로 연결된다.

| 항목 | 내용 |
|------|------|
| 대상 시장 | 국내(KR) + 해외(US) |
| 국내 배당 데이터 | 한국투자증권(KIS) Open API |
| 해외 배당 데이터 | yfinance |
| 보유 종목 관리 | KIS 잔고조회(자동) + 수동 입력 둘 다 지원 |
| 캘린더 표시 기준 | 지급(예정)일 `pay_date` |
| 예상 배당금 | 주당 배당금 × 보유 수량 |

---

## 2. 자동화 가능성 — 결론: 가능

- **국내 주식**: KIS API가 잔고조회와 예탁원정보(배당일정)를 모두 제공한다.
  보유 종목 목록부터 배당 일정까지 사람 손 없이 한 번에 수집 가능.
- **해외 주식**: KIS는 국내 배당 위주이므로 yfinance(무료)로 별도 수집.
  종목별 배당 이력과 예정일을 제공한다.

### 데이터 흐름

```
[1] 보유 종목 등록 (수동 입력 or KIS 잔고조회 자동)
        ↓
[2] 스케줄러(APScheduler/cron)가 하루 1회 배당 API 호출
    - 국내: KIS 예탁원정보(배당일정)
    - 해외: yfinance
        ↓
[3] MySQL 저장 (dividends 테이블)
    종목코드 / 배당기준일 / 지급예정일 / 주당배당금 / 보유수량
        ↓
[4] 예상 배당금 = 주당배당금 × 보유수량 (자동 계산)
        ↓
[5] Next.js 캘린더에 지급예정일 기준으로 표시
        ↓
[6] (선택) 지급일에 가계부 "수입" 으로 자동 반영
```

> **완전 자동화의 핵심은 [2]번 스케줄러.** 배당 일정은 자주 바뀌지 않으므로
> 하루 1회 갱신으로 충분하고, 캘린더는 DB만 읽으면 되어 빠르다.

### 주의: 예상 vs 확정

미래 배당은 작년 실적 기반 추정치라 실제와 다를 수 있다.
`dividends.status` 컬럼(`estimated` / `confirmed`)으로 구분해 캘린더에
표시하면 신뢰도가 올라간다.

---

## 3. 기술 스택 (기존 환경 재사용)

- **프론트엔드**: Next.js — 캘린더 UI
- **백엔드**: FastAPI — 배당 수집·계산 API
- **DB**: MySQL — 보유종목 / 배당일정 저장
- **스케줄러**: APScheduler 또는 cron — 일 1회 자동 갱신
- **외부 API**: KIS Open API(국내), yfinance(해외)

```bash
pip install requests yfinance
```

---

## 4. DB 스키마 (MySQL 8.0+)

`dividend_schema.sql` 참조. 핵심 3요소.

### 4-1. `holdings` — 보유 종목
- `source` 컬럼(`kis` / `manual`)으로 자동 동기화분과 수동 입력분 분리
- KIS 동기화 시 수동 입력한 해외주식을 덮어쓰지 않도록 분리 관리
- `quantity` 는 `DECIMAL(18,4)` — 해외 소수점 주식 대응

### 4-2. `dividends` — 배당 일정
- `(symbol, pay_date, dividend_per_share)` 단위 관리 (분기/반기 배당 대응)
- `status` 로 예상/확정 구분 → 캘린더 색상/표시 차별화
- `currency` 로 KRW / USD 구분

### 4-3. `v_dividend_calendar` — 캘린더 조회용 뷰
- 보유 종목과 배당 일정을 조인
- `expected_amount = dividend_per_share × quantity` 자동 계산

---

## 5. 백엔드 핵심 모듈

`dividend_service.py` 참조. 세 부분으로 구성.

| 구성 요소 | 역할 |
|-----------|------|
| `KISClient` | 토큰 발급·캐싱, 잔고조회(보유종목 자동 수집), 국내 배당일정 조회 |
| `fetch_dividend_us()` | yfinance 로 해외 배당 이력 + 예정 배당 조회 |
| `DividendService.collect()` | 보유종목(자동+수동) × 배당금 → 예상 수령액 계산 |

### FastAPI 라우터 연결 예시

```python
@router.post("/dividends/sync")
async def sync_dividends(user_id: int, db: Session = Depends(get_db)):
    service = DividendService(kis=get_kis_client(user_id))
    rows = service.collect(manual_holdings=load_manual_holdings(db, user_id))
    upsert_dividends(db, rows)   # 기존 DB 레이어로 저장
    return {"synced": len(rows)}
```

### ⚠️ KIS API 확인 필요 지점

토큰 발급과 잔고조회는 표준 규격이라 그대로 동작한다.
단, **예탁원정보(배당일정)** 부분은 계정·문서 버전에 따라 다를 수 있으므로
아래를 KIS 공식 문서(https://apiportal.koreainvestment.com)에서 대조할 것.

- 엔드포인트: `/uapi/domestic-stock/v1/ksdinfo/dividend`
- `tr_id`: `HHKDB669102C0`
- 응답 필드명: `per_sto_divi_amt`, `divi_pay_dt`, `record_date` 등

---

## 6. 개발 로드맵

| 단계 | 내용 | 상태 |
|------|------|------|
| 1단계 | 데이터 수집 (DB 스키마 + 배당 조회 서비스) | ✅ 완료 |
| 2단계 | 스케줄러 자동 갱신 (APScheduler, 일 1회) | ⬜ 예정 |
| 3단계 | Next.js 캘린더 UI | ⬜ 예정 |
| 4단계 | 가계부 "수입" 자동 연동 | ⬜ 예정 |

> 1~2단계만 완료해도 "배당 일정이 자동으로 채워지는 캘린더" 목표는 달성.

---

## 7. 산출물

- `dividend_schema.sql` — MySQL 스키마 (holdings, dividends, 캘린더 뷰)
- `dividend_service.py` — 배당 수집 서비스 (KIS 국내 + yfinance 해외)
- `배당캘린더_설계문서.md` — 본 문서

---

## 8. 통합 시 참고

이 기능은 기존 가계부·주식 프로그램 코드베이스에 직접 통합하는 것이 핵심이다.
- DB 연결·인증 구조는 기존 레이어 재사용
- `DividendService` 는 순수 조회·계산만 담당하고, 저장은 호출측(기존 DB 레이어)에서 처리
- 로컬 코드베이스에서 직접 파일을 읽고 수정하는 작업이 많으므로 Claude Code 활용 권장
